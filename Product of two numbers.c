#include <stdio.h>
main()
{
    int N1, N2, M;
    printf("I will multiply two numbers together \nType the first number : ");scanf("%d", &N1);
    printf("Type the second number : ");scanf("%d", &N2);
    M = N1 * N2;
    printf("The result is : %d", M);
}
